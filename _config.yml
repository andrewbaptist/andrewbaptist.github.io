# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely need to edit after that.
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'jekyll serve'. If you change this file, please restart the server process.

# Site settings
title: "Object Everywhere"
email: abaptist@us.ibm.com
description: > # this means to ignore newlines until "baseurl:"
    Evangilizing about the benefits of object storage and where 
    the future of object storage will be.
baseurl: "" # the subpath of your site, e.g. /blog
lang: "en"
url: "http://objecteverywhere.com" # the base hostname & protocol for your site

# Build settings
markdown: kramdown
permalink: /:categories/:title/

# Posts settings
future: true
safe: false
lsi: false
excerpt_separator: "\n\n"

# Theme settings
compress_html:
  comments: ["<!-- ", " -->"]
  clippings: all
  endings: all


sass:
  sass_dir: assets/_sass
  style: compressed


images: '/assets/images'

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag
